-- ==========================================================================
-- Нагадування про безкоштовні кредити — знайдено автономним аудитом
-- 2026-09-15 (мандат "ціль прибуток"): з 84 користувачів лише 21 (25%)
-- хоч раз згенерували документ, 63 досі мають незайманими свої 2
-- безкоштовних кредити — не активувались взагалі. Це найбільша знайдена
-- діра в лійці саме для TeacherPlan (на відміну від tender-intel, де
-- проблема була в монетизації вже активних користувачів).
--
-- activation_reminder_sent_at — щоб не слати одному й тому ж користувачу
-- нагадування повторно (скрипт scripts/send-activation-reminders.ts).
-- Сам скрипт НЕ запускається автоматично (нема cron) — свідомо, лист
-- реальним користувачам має спершу побачити людина, перш ніж він піде
-- в розсилку.
-- ==========================================================================
alter table public.profiles
  add column if not exists activation_reminder_sent_at timestamptz;
