# TeacherPlan - Оновлена версія з усіма предметами

## 📚 Додані предмети

### Вже реалізовані:
1. ✅ **Фізична культура** (5 клас, НУШ) - ГОТОВО
2. ✅ **Українська мова** (5-11 класи) - ГОТОВО
3. ✅ **Історія: Україна і світ** (10-11 класи) - ГОТОВО (нові модулі створено)

### Потребують завершення:
4. 🔧 **Інформатика** (10-11 класи, стандарт)
5. 🔧 **Математика** (10-11 класи: стандарт, поглиблений, профільний)
6. 🔧 **Мистецтво** (10-11 класи, профільний рівень)

## 🎯 Що зроблено

### Історія: Україна і світ
- ✅ Створено файл `history-modules.ts` з модулями для 10-11 класів
- ✅ Створено файл `history-plan.ts` з логікою генерації
- ✅ Додано детальний зміст уроків на основі навчальної програми МОН України
- ✅ 10 клас: 7 розділів (62 години)
- ✅ 11 клас: 6 розділів (58 годин)

## 📝 Структура модулів для історії

### 10 клас (1914-1945 рр.):
1. Вступ. Передодень світової війни (3 год)
2. Перша світова війна (8 год)
3. Розпад імперій та утворення національних держав (10 год)
4. Облаштування повоєнного світу (9 год)
5. Тоталітарні режими (11 год)
6. Україна і світ напередодні Другої світової війни (8 год)
7. Друга світова війна (13 год)

### 11 клас (від 1945 р. до сучасності):
1. Вступ. Уроки Другої світової війни (2 год)
2. Повоєнне врегулювання (6 год)
3. Світ у пошуках нових моделей розвитку (10 год)
4. Україна в період відлиги (7 год)
5. Формування постіндустріального суспільства (11 год)
6. Революційні зміни в Європі (9 год)
7. Україна в сучасному світі (13 год)

## 🔄 Наступні кроки

### Для завершення проекту потрібно:

1. **Інформатика (10-11):**
   - Створити `informatics-modules.ts`
   - Створити `informatics-plan.ts`
   - Модулі: Базовий модуль (35 год) + вибіркові модулі

2. **Математика (10-11):**
   - Створити `mathematics-modules-standard.ts`
   - Створити `mathematics-modules-advanced.ts`
   - Створити `mathematics-modules-profile.ts`
   - Створити `mathematics-plan.ts`
   - Розділи: Алгебра і початки аналізу + Геометрія

3. **Мистецтво (10-11):**
   - Створити `art-modules.ts`
   - Створити `art-plan.ts`
   - Профільний рівень з практичними заняттями

4. **Оновити API:**
   - Додати routes для всіх нових предметів
   - Оновити `app/api/generate/route.ts`

5. **Оновити UI:**
   - Додати вибір предметів у форму
   - Додати вибір рівня (для математики, інформатики, мистецтва)
   - Оновити компонент GenerateForm

## 📂 Структура файлів

```
lib/generation/
├── calendar-plan.ts          # Фізкультура (5 клас)
├── ukrainian-plan.ts         # Українська мова (5-11)
├── ukrainian-modules.ts      # Модулі української мови
├── history-plan.ts           # ✅ Історія (10-11) - НОВИЙ
├── history-modules.ts        # ✅ Модулі історії - НОВИЙ
├── informatics-plan.ts       # 🔧 Потрібно створити
├── informatics-modules.ts    # 🔧 Потрібно створити
├── mathematics-plan.ts       # 🔧 Потрібно створити
├── mathematics-modules-*.ts  # 🔧 Потрібно створити
├── art-plan.ts              # 🔧 Потрібно створити
├── art-modules.ts           # 🔧 Потрібно створити
└── all-subjects.ts          # ✅ Реєстр усіх предметів
```

## 🚀 Використання

### Генерація календарного плану:

```typescript
import { generateHistoryCalendarPlan, generateHistoryHTML } from '@/lib/generation/history-plan';

const settings = {
  class: '10',
  subject: 'Історія: Україна і світ',
  schoolYear: '2024/2025',
  semester: '0', // 0 = весь рік, 1 = перший семестр, 2 = другий семестр
  weekdays: 'Пн,Ср', // Дні тижня для уроків
  startDate: '2024-09-02',
  teacherName: 'Іваненко І.І.',
  teacherCategory: 'вчитель вищої категорії',
  schoolName: 'Загальноосвітня школа №1'
};

const result = await generateHistoryCalendarPlan(settings);
if (result.success) {
  const html = generateHistoryHTML(result.lessons, settings);
  // html тепер містить готовий календарний план
}
```

## 📋 TODO List

- [ ] Створити модулі для інформатики
- [ ] Створити модулі для математики (3 рівні)
- [ ] Створити модулі для мистецтва
- [ ] Оновити API routes
- [ ] Оновити форму генерації
- [ ] Додати тести
- [ ] Оновити Google Apps Script

## 💡 Примітки

- Всі модулі базуються на офіційних навчальних програмах МОН України
- Детальний зміст уроків генерується автоматично на основі теми та розділу
- Підтримка розподілу по семестрах
- Автоматичне форматування дат та нумерація уроків
- Експорт у HTML формат для подальшого перетворення в DOCX

  {
    name: "Мистецтво культурних регіонів світу",
    hours: 25,
    topics: [
      "Мистецтво Стародавнього Сходу",
      "Античне мистецтво",
      "Мистецтво Середньовіччя",
      "Мистецтво епохи Відродження",
      "Бароко та класицизм",
      "Романтизм та реалізм",
      "Імпресіонізм та постімпресіонізм",
      // ... додати всі теми
    ]
  },
  {
    name: "Практичні заняття з образотворчого мистецтва",
    hours: 20,
    topics: [
      "Малюнок олівцем. Основи композиції",
      "Живопис акварелю. Кольорознавство",
      "Графічні техніки",
      // ... практичні завдання
    ]
  }
];
```

---

## КРОК 4: Оновити API маршрути

### Файл: `app/api/generate/route.ts`

Додати обробку всіх нових предметів:

```typescript
import { generateHistoryCalendarPlan, generateHistoryHTML } from '@/lib/generation/history-plan';
import { generateInformaticsCalendarPlan, generateInformaticsHTML } from '@/lib/generation/informatics-plan';
import { generateMathematicsCalendarPlan, generateMathematicsHTML } from '@/lib/generation/mathematics-plan';
import { generateArtCalendarPlan, generateArtHTML } from '@/lib/generation/art-plan';

export async function POST(request: Request) {
  const data = await request.json();
  
  let result;
  let html;
  
  switch(data.subject) {
    case 'Історія: Україна і світ':
      result = await generateHistoryCalendarPlan(data);
      if (result.success) {
        html = generateHistoryHTML(result.lessons, data);
      }
      break;
      
    case 'Інформатика':
      result = await generateInformaticsCalendarPlan(data);
      if (result.success) {
        html = generateInformaticsHTML(result.lessons, data);
      }
      break;
      
    case 'Математика':
      result = await generateMathematicsCalendarPlan(data);
      if (result.success) {
        html = generateMathematicsHTML(result.lessons, data);
      }
      break;
      
    case 'Мистецтво':
      result = await generateArtCalendarPlan(data);
      if (result.success) {
        html = generateArtHTML(result.lessons, data);
      }
      break;
      
    // ... існуючі предмети
  }
  
  return Response.json({ success: true, html });
}
```

---

## КРОК 5: Оновити форму генерації

### Файл: `app/dashboard/page.tsx` або компонент форми

Додати:

1. **Dropdown з усіма предметами:**
```typescript
const subjects = [
  'Фізична культура',
  'Українська мова',
  'Історія: Україна і світ',
  'Інформатика',
  'Математика',
  'Мистецтво'
];
```

2. **Умовний вибір рівня** (для математики, інформатики, мистецтва):
```typescript
{subject === 'Математика' && (
  <select name="level">
    <option value="стандарт">Рівень стандарту</option>
    <option value="поглиблений">Поглиблений рівень</option>
    <option value="профільний">Профільний рівень</option>
  </select>
)}
```

3. **Умовний вибір класу** (залежно від предмета):
```typescript
const getClassesForSubject = (subject: string) => {
  switch(subject) {
    case 'Фізична культура':
      return ['5'];
    case 'Українська мова':
      return ['5', '6', '7', '8', '9', '10', '11'];
    case 'Історія: Україна і світ':
    case 'Інформатика':
    case 'Математика':
    case 'Мистецтво':
      return ['10', '11'];
    default:
      return [];
  }
};
```

---

## КРОК 6: Оновити Google Apps Script

### Файл: `GOOGLE_APPS_SCRIPT.js`

Додати функції для всіх нових предметів:

```javascript
function doPost(e) {
  const data = JSON.parse(e.postData.contents);
  
  switch(data.subject) {
    case "Історія: Україна і світ":
      return ContentService.createTextOutput(
        JSON.stringify(generateHistoryPlan(data))
      ).setMimeType(ContentService.MimeType.JSON);
      
    case "Інформатика":
      return ContentService.createTextOutput(
        JSON.stringify(generateInformaticsPlan(data))
      ).setMimeType(ContentService.MimeType.JSON);
      
    case "Математика":
      return ContentService.createTextOutput(
        JSON.stringify(generateMathematicsPlan(data))
      ).setMimeType(ContentService.MimeType.JSON);
      
    case "Мистецтво":
      return ContentService.createTextOutput(
        JSON.stringify(generateArtPlan(data))
      ).setMimeType(ContentService.MimeType.JSON);
      
    // ... існуючі
  }
}

function generateHistoryPlan(data) {
  // Аналогічно до generateUkrainianPlan
  const doc = DocumentApp.create(`Календарний план: Історія ${data.class} клас`);
  // ... генерація документу
}

// ... аналогічно для інших предметів
```

---

## КРОК 7: Тестування

1. Перевірити генерацію для кожного предмета
2. Перевірити всі класи (5-11)
3. Перевірити різні рівні (стандарт/поглиблений/профільний)
4. Перевірити розподіл по семестрах
5. Перевірити експорт у Google Docs

---

## 📋 ЧЕКЛИСТ ДЛЯ ВИКОНАННЯ:

### Інформатика:
- [ ] Створити `informatics-modules.ts` з базовим модулем (35 год)
- [ ] Додати 3-4 вибіркові модулі (по 15-20 год кожен)
- [ ] Створити `informatics-plan.ts` з логікою генерації
- [ ] Додати функцію `generateInformaticsHTML()`
- [ ] Протестувати генерацію

### Математика:
- [ ] Створити `mathematics-modules.ts` з 3 рівнями
- [ ] Стандартний рівень: Алгебра (40 год) + Геометрія (30 год)
- [ ] Поглиблений рівень: Алгебра (60 год) + Геометрія (45 год)
- [ ] Профільний рівень: Алгебра (80 год) + Геометрія (55 год)
- [ ] Створити `mathematics-plan.ts` з логікою генерації
- [ ] Додати вибір рівня у формі
- [ ] Протестувати всі 3 рівні

### Мистецтво:
- [ ] Створити `art-modules.ts` для профільного рівня
- [ ] Теорія мистецтва + Практичні заняття
- [ ] Створити `art-plan.ts` з логікою генерації
- [ ] Протестувати генерацію

### API та UI:
- [ ] Оновити `app/api/generate/route.ts`
- [ ] Оновити форму вибору предмета
- [ ] Додати умовний вибір рівня
- [ ] Додати умовний вибір класу
- [ ] Оновити Google Apps Script

### Документація:
- [ ] Оновити README.md з прикладами
- [ ] Додати screenshots
- [ ] Створити CHANGELOG.md

---

## 🎯 ПРІОРИТЕТИ:

**Висок ий пріоритет:**
1. Математика (найпопулярніший предмет)
2. Інформатика (обов'язковий у багатьох школах)

**Середній пріоритет:**
3. Мистецтво (тільки профільні класи)

---

## 💡 ПОРАДИ:

1. **Копіюйте структуру з history-plan.ts** - вона вже працює ідеально
2. **Використовуйте офіційні програми МОН** - всі файли є у вкладеннях
3. **Генерація змісту** - адаптуйте під кожен предмет:
   - Математика: задачі, формули, доведення
   - Інформатика: практичні роботи, програмування
   - Мистецтво: творчі завдання, аналіз творів

4. **Тестуйте послідовно:**
   - Спочатку модулі (чи правильна структура)
   - Потім генерація (чи працює логіка)
   - Потім UI (чи зручно користувачу)
   - Потім експорт (чи коректний документ)

---

## 📞 Контакти

Для питань та пропозицій створюйте Issues в репозиторії.
